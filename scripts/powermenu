#!/usr/bin/env bash

choice=$(printf '%s\n' \
    "󰐥 Shutdown" \
    " Restart" \
    " Lock" \
    "󰤄 Sleep" |
    dmenu -c -bw 3 -i -l 4 -p "Power:")

case "$choice" in
    "󰐥 Shutdown")
        loginctl poweroff 
        ;;
    " Restart")
        loginctl reboot
        ;;
    " Lock")
        slock
        ;;
    "󰤄 Sleep")
        loginctl suspend
        ;;
esac
