#!/bin/sh
set -eu

# Bookmark file
BOOKMARKS_FILE="${BOOKMARKS_FILE:-$HOME/dotfiles/.config/bookmarks/bookmarks.txt}"

# dmenu look
DMENU="dmenu -i -bw 3 -z 1914 -l 10 -p 'Bookmarks:'"

# Browsers
BROWSER="$(command -v waterfox || true)"
FALLBACK="$(command -v xdg-open || echo waterfox)"

# Ensure bookmark directory/file exists
mkdir -p "$(dirname "$BOOKMARKS_FILE")"

[ -f "$BOOKMARKS_FILE" ] || cat >"$BOOKMARKS_FILE" <<'EOF'
# Bookmarks
# Format:
# name :: https://example.com
#
# You can also just use:
# https://example.com
EOF

# Build bookmark list
emit() {
  grep -vE '^\s*(#|$)' "$BOOKMARKS_FILE" |
  while IFS= read -r line; do
    case "$line" in
      *"::"*)
        lhs="${line%%::*}"
        rhs="${line#*::}"

        lhs="$(printf '%s' "$lhs" | sed 's/[[:space:]]*$//')"
        rhs="$(printf '%s' "$rhs" | sed 's/^[[:space:]]*//')"

        printf '%s :: %s\n' "$lhs" "$rhs"
        ;;
      *)
        printf '%s :: %s\n' "$line" "$line"
        ;;
    esac
  done
}

# Select bookmark
choice="$(
  emit |
  sort |
  eval "$DMENU" ||
  true
)"

[ -n "$choice" ] || exit 0

# Extract URL
url="${choice##* :: }"

# Strip inline comments and trim
url="$(printf '%s' "$url" |
  sed -e 's/[[:space:]]\+#.*$//' \
      -e 's/[[:space:]]\/\/.*$//' \
      -e 's/^[[:space:]]*//' \
      -e 's/[[:space:]]*$//')"

# Ensure scheme
case "$url" in
  http://*|https://*|file://*|about:*|chrome:*)
    ;;
  *)
    url="https://$url"
    ;;
esac

# Open bookmark
open_with() {
  cmd="$1"

  if [ -n "$cmd" ]; then
    nohup "$cmd" --new-tab "$url" >/dev/null 2>&1 &
    exit 0
  fi
}

open_with "$BROWSER"

# Fallback
nohup "$FALLBACK" "$url" >/dev/null 2>&1 &
