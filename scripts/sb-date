#!/bin/sh

STATE_FILE=/tmp/dwmblocks_date_state
STATE=$(cat "$STATE_FILE" 2>/dev/null || echo 0)

# Left click toggles Hijri (0) / Gregorian (1)
if [ "$BLOCK_BUTTON" = 1 ]; then
    STATE=$((1 - STATE))
    echo "$STATE" > "$STATE_FILE"
fi

if [ "$STATE" = 1 ]; then
    get_hijri g
else
    get_hijri
fi
